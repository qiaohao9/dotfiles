#!/bin/bash

# replace node_modules
case $(uname) in
    "Darwin")
        sed -i "" -e "s#prefix=.*#prefix=${HOME}/.node_modules#" ${HOME}/.npmrc
        ;;
    "Linux")
        sed -i -e "s#prefix=.*#prefix=${HOME}/.node_modules#" ${HOME}/.npmrc
        ;;
    *)
        ;;
esac
